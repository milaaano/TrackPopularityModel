# Backend runtime configuration.
# Obtain a key at https://www.last.fm/api/account/create and store the real value
# as a Hugging Face Space secret, never in this file.
LASTFM_API_KEY=

# Exact browser origins allowed to call the API, separated by commas.
ALLOWED_ORIGINS=http://localhost:3000
