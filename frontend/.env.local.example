# Base URL of the FastAPI backend (no trailing slash).
# Local dev:            http://localhost:8000
# Hugging Face Space:   https://<your-space>.hf.space   (port 7860 behind the proxy)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
